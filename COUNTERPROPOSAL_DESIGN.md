# 可执行修正候选，而不是更多解释文字

统一实验运行中，对3条已结束的拒绝轨迹作离线诊断：仅凭本run已有签名物流报价，可构造3个只改价格的候选。记录在results/repair_v10_unified/public_repair_diagnostics.json。该诊断不读取私有注册表，也没有执行候选；不能宣称3个候选满足所有组织约束或都将被Agent接受。

新增public_quote_repair.py，先验签并绑定工作流，保持批次、数量、路线和时间不变，只按签名服务报价与共享计费规则计算价格；缺少报价、时间不符或公开容量不够时不返回候选。结果显式携带base_plan_hash和authority_offer_hash，类型是counterproposal_not_authorized。

拟接入的协议变化是将否认转化为“受限、有来源的反提案”：组织事实决定允许的修正范围，程序生成候选，Agent负责业务选择，执行网关仍要求新的买方意愿与受影响批准。这区别于让LLM自由重写整份计划，也区别于后台擅自替买方执行修正方案。

当前统一18run冻结不变，新模块仅用于事后诊断。下一版本若采用候选机制，必须把候选工具给全部对照组，单独测自动何时触发和哪些依赖重验，不能把算术工具只交给新组制造优势。通用性仍需验证；修正金额本身不是研究创新，价值要来自跨组织的权限边界、依赖闭包和可靠恢复协议。
