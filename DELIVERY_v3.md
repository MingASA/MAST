> **第三轮修订状态：原12-run自然流程方案已暂停。**现已改成暴露受控的银行入口实验；受控批次已完成，实际结果见文末。历史暴露为0/44；本实验不能用于估计自然冲突发生/暴露概率。实现说明见MODEL_DECISIONS.md末节，输入与预算见results/framing_v3_controlled_preflight。

# 第三轮交付：实现完成，暴露受控批次已执行

本轮新增银行遗漏责任framing与真实冲突检出评估，完成现有四图的下游价值集中度分析。原自然流程方案没有执行；修订后的暴露受控MiniMax批次已在用户确认后执行，结果见下文。

## 主线实现与审查

- [framing.py](trust_network/demo/framing.py)：A六组织原resp；B仅export_bank、issuing_bank追加一条一般遗漏责任说明。对应图节点export_bank_review、issuing_review。固定protocol2，其余系统提示、组织档案、初始资料和执行流程不变。
- [MODEL_DECISIONS.md](MODEL_DECISIONS.md)：已在执行前保存两银行A/B完整system、objective、task原文及逐句diff；说明旧提示原本含业务线索，新增句没有型号、版本、发票或具体检查对象。[可读取快照](results/framing_v3_preflight/prompt_review.md)。
- [discrepancy.py](trust_network/demo/discrepancy.py)：从审计日志重放银行输入并核验哈希，用统一的条件盲态语义抽取识别银行findings中的实际冲突发现；阳性须有原文引用及真实单据字段绑定。没有暴露时无需额外模型调用。保存抽取原始响应，支持复核，未将语义标注器视为无误判真值。
- [run_framing.py](trust_network/demo/run_framing.py)：各组6次、温度0.2/0.5/0.8各2次；默认只估算。主报告检出次数、有效分母、描述性Wilson区间、B−A差、实际暴露情况；业务结局、重提交、检查范围作为背景。失败标签为null，分开记录，不补跑、不计入主分母。每次批次必须使用新的结果目录。

当前是责任文字框架的真实LLM行为检验入口，没有把数值责任结算、实际扣款或责任承诺执行机制接入业务运行。观察到提示词行为变化也不自动证明整个机制在真实组织中有效。

## 执行前发现：银行可能没有检测机会

[历史回放](results/framing_v3_preflight/historical_bank_exposure.json)覆盖第二轮30个run、44次银行决策，**原始未修正冲突的银行暴露次数为0**。卖方已在银行前修改发票，另一些流程提前终止。

严格沿用原流程的A/B批次可能产生双组零检出、零暴露；这只能说明没有观察到银行面对该冲突的判断，不能说明遗漏责任无效或银行无检测能力。没有为了制造可测差异而改变卖方行为或角色顺序。后续若做暴露受控的银行审查变体，必须明确作为另一个实验，两组使用相同未修正输入，单独报告。

唯一主指标仍为flagged_invoice_discrepancy；bank_exposed是解释其是否有检测机会的诊断量。零暴露时条件检出率为NA。网络、无效响应、评估错误均单列，有失败时报告k/有效n，并同时列预定6次，而非偷偷将错误算未检出。

## 预算及尚未完成项

预算基于第二轮相同protocol2/resp组的15次run、85次组织决策、180854 token。

|项目|逻辑调用|token估算/预留|
|---|---:|---:|
|12次工作流|约68|144683|
|额外责任文本及上下文传播|包含在工作流|3000|
|有暴露时的事后抽取|至多12|36000|
|合计规划|约80|183683|

如果仍无银行暴露，抽取为0，预计接近68次调用、14.8万token。以上不是上限或账单，格式重试与失败请求可能额外消耗。没有假设模型价格或给出未经依据的金额。[机器可读估算](results/framing_v3_preflight/estimate.json)。

原自然流程12-run方案已被修订任务书暂停。用户确认固定受控输入和预算后，实际结果见下方“实际受控批次结果”；没有以历史回放或离线测试冒充第三轮真实实验。

这是遗漏责任机制第一次在真实agent上做的小规模检验，不是决定性证据，更大样本或更多场景变体是后续工作。N=6/组不足以下因果或统计显著性结论，只能描述观察方向与幅度。

## 次线：四图探索性对比已完成

固定所有verify为pass，定义`C(v)=E[1{访问v}×v之后严格下游损失]/E[全路径损失]`。包含到达概率及原模型终止罚；这是价值位置，不是该节点实际可挽救损失。精确积分既有图参数，PoA直接读第二轮结果，未新增图或重跑sweep。[完整定义和分析](results/structure_v3/report.md)，[全部14个验证节点](results/structure_v3/nodes.csv)。

|场景|最大C|平均C|PoA w=0 → w=1|绝对改善|
|---|---:|---:|---|---:|
|随机7|1|1|1.159031 → 1.159031|约0|
|随机11|1|1|1.122889 → 1.122889|0|
|随机23|1|1|1.089577 → 1.089577|约0|
|信用证|1|0.309888|2.638268 → 1.006474|1.631794|

四图都有损失前的必经验证节点，最大集中度无区分能力；当前对比没有为瓶颈假说提供区分性支持，也不足以否定。信用证较低的平均C受低到达率重提交副本影响。n=4只是探索性观察，无统计检验。未来需要有绕行分支、审查前或其他支路损失的场景，分开控制成本、修复能力及责任份额。

## 必要验证与复现

原42项加新增6项，共**48项通过（4.30秒）**。新增验证覆盖实际runner中A/B仅银行目标字段不同、修复前后真实暴露、抽取引用/文档绑定、无暴露不调用模型、失败分母处理、带分支及新生错误的解析集中度。没有运行额外大规模测试或扫描。

原有209个结果及测试文件哈希保持不变：[保留检查](results/round3_preflight/preservation_check.json)。复现命令见[README第三轮说明](README.md#第三轮增量工作)。

## 实际受控批次结果

用户确认后已使用 `/home/cjy/cyberagent/.env` 执行受控批次。第一次尝试因沙箱网络限制在首个调用前全部失败，保留于 `results/framing_v3_controlled`；放行 HTTPS 访问后实际结果保留于 [results/framing_v3_controlled_retry](results/framing_v3_controlled_retry)。第一次尝试没有产生模型调用或token，不计入实验样本。

有效结果为 A 组 5/6、B 组 6/6；A 为 5/5 检出，B 为 6/6 检出，均为100%，暴露条件下也是100%。A组1个run在出口方银行请求补证后工作流失败，按预注册规则没有伪造标签，也没有计入分母。有效run均在出口方银行首次审单时暴露并升级，开证行没有取得独立决策机会；因此本批次不是两家银行在相同输入上的独立对照。两组有效run业务结局均为人工升级、无重提交。

观测到的 B−A 检出率差为0个百分点。主指标在这个固定输入上达到天花板，不能回答遗漏责任句是否带来增量检出效果；只能说明两种提示框架下，模型都能识别并指出这个直接呈现的型号冲突。A组有效run平均记录4.6项检查，B组3.83项，属于背景描述，不能据此解释机制效果。

实际记录12个工作流组织调用、11个事后评估调用，共23次逻辑调用、23521 token；低于预估是因为大多数run在首个银行决策即升级。详细温度分层、检查范围、失败状态和usage见批次目录。该结果仍属于暴露受控预实验，不代表自然流程中的冲突暴露概率，也不能证明数学遗漏责任机制已经在真实agent系统中生效。


## 修订补丁交付（当前有效状态）

原自然流程12-run计划已暂停，第三轮入口已切换为暴露受控。固定输入是原公开包：LC-1及PK-1为MX-40，INV-2为MX-40B，数量10；原提单、有限检验范围及未完成采购审批邮件不变。离线核对ground_truth.json中的授权/实物型号及卖方提前修改事故一致。文件见[固定输入](examples/letter_of_credit/controlled_bank_input.json)。

从原角色序列的银行入口启动，初始无前置消息或证书；两组相同。卖方等组织仍可被银行按原逻辑请求补证、改单、重审。真实修正后不重新注入冲突；开证行是否仍面对冲突取决于出口方银行及后续真实动作。角色相对顺序、两种framing和协议规则保留。主指标评估改为从固定输入重放，报告标注“暴露受控”，并列历史0/44及温度分层。

预算约68次工作流调用/144683 token，加责任文本3000及最多12次抽取/36000 token，规划合计约80次调用/183683 token；省去前置调用但可能增加补证，故保留历史量级而不机械扣减。仅为估算，不是上限。

必要验证：**50项通过（3.94秒）**；新增覆盖相同A/B入口、真实补证改单后不重置错误、下游角色顺序和日志暴露重放，以及固定包与真值一致。209个既有结果/测试文件仍未变化。真实批次的实际用量和失败状态见下方结果目录。

待用户确认修订后的固定输入和预算后，执行N=6/组的受控批次并补实际结果。这批数据只回答银行面对冲突时framing是否改变其检查/查证行为，不能用于估计自然流程暴露概率；小样本不能证明因果或统计显著性。
