# 统一比较设计

2次重复 × 3条件（无变化、费率变化、费率与供应批次双变）× 3策略，共18run。使用run_repair_comparison，执行前写入源码与案例摘要，按重复/条件轮换策略顺序。

Autonomous、整计划全验证、依赖阶段验证共享业务提示、模型设置、3轮协商上限、组织补证接口与版本化repair envelope。区别仅在程序强制验证及证据复用/阶段调度。Autonomous保留可选verify。结构化消息不是只给新机制组的额外帮助。

主指标为按最终私有状态事后评估的不安全完成率、安全完成率；同时记录查询、模型调用、token、拒绝、协商轮数及错误否认。统计按条件与策略分别报告，不能用无变化的正常样本掩盖变化条件失败。拒绝不计安全完成。

预计基础54次模型调用；发生修正时每轮可能增加2次组织补报价及1次买方调用，18run上限约162次（不含provider格式重试和初始无效报价重试），约5万—20万token，具体以日志为准。该规模为探索对照，不保证统计显著性。

受控事件不伪装为自然错误率；临时私有状态由实验环境更新，不提供给策略。动态资源预留不包含在本批报价恢复问题中，不能声称已经同时验证跨订单容量正确性。本批不以更换提示或增加轮数救回失败样本。
