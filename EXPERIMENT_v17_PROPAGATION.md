# 接收前阻断与提交前阻断的区别

使用本次真实协调Agent生成的同一总价声明，受控构造金额改错和漏掉依赖两个反例，保留原始无故障条件。三个程序控制对照为：只验签、到提交时验证派生关系、在接收交付前验证派生关系。动作合约的订单授权和金额上限相同。

主要观察不是token，而是错误声明是否已被交付给接收边界，以及最后是否触发错误确认。预期提交前验证和接收前验证都阻止错误动作，但后者还能阻止错误声明进入下游输入边界。若只检查最后动作，就看不到这个区别。

故障包由实验环境重新签署并明确标为受控修改，不能归因于原始MiniMax输出。对照程序在进程内重放，一次下游暴露是仪器化的交付指标，不是自然模型采信率；完整真实流程仍在独立组织进程运行。只验签是弱对照，其失败不代表优于所有现有系统。撤销后的账单重放仍由真实运行器记录。
