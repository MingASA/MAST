# 给自主检查同样的依赖信息

上一批模型只看到fact，没有parent关系，因此遗漏依赖的转发不能解释为模型明知证据不完整仍转发。新批次给两组相同的claim_id、issuer、parents、rule和fact，并说明sum_charges与relay的检查规则，仍允许模型自行hold。

正常、金额改错、遗漏依赖各比较提交时验证与接收前验证，最多两跳真实MiniMax接收。源数据仍为同一真实账单的显式受控签名修改，不能算自然错误率。若模型自行挡住全部错误，就报告无新增转发阻断收益；不再通过隐藏依赖关系让基线吃亏。

上一批第二跳运行错误因只保存generic runtime_error，不能从现存记录确定根因。新增错误阶段记录，区分provider异常与模型输出动作不符合schema，并保留后者的raw draft和usage。没有证据证明旧错误已被修好；新的批次单独记录，不覆盖旧结果。
