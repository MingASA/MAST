# 依赖范围证据对照：执行前冻结

本批回答一个有限问题：在相同三组织业务流程、模型和owner检查下，按依赖字段绑定证据，是否比整计划绑定证据减少计划修正时的重新确认，同时保持最终可行性与正常完成。

这是协议绑定粒度对照，不是Autonomous可靠性主对照，也不是一般风险策略最优性证明。此前pilot用于发现报价错误与修复日志问题，不混入本批样本。

## 固定矩阵与变量

3个预先生成案例（economy、urgent、not_approved）×2种绑定（full、dependency）×2次重复，共12run。每个case/重复交替执行绑定条件，温度沿用共同provider配置0.2；共享组织档案、私有资源、模型prompt、工具、尝试上限和TTL。无故障注入、无失败自动重跑、无中途改prompt或调参。

两组owner_check完全相同。full证书绑定完整计划；dependency证书只绑定该owner依赖字段。两组均缓存仍有效证据，相同计划重复检查不重新收费。计划改价时，full原证书不能覆盖改后的计划；dependency供应证书仍覆盖未变供货安排，因此可以复用。这个真实绑定差异是处理变量，不是人为禁用对照缓存。

## 指标与解释

每组报告：尝试数/错误数、已授权正常完成、未授权最终提交、最终硬约束违反、承诺查询次数、模型调用和token、实际修正次数、计划质量。错误run单列，不当安全完成；未知usage不推断零费用。

成本主比较使用每尝试run查询数，并给出按case/重复的配对差值；补充相同实际草稿轨迹上的绑定重放，明确后者不是完整独立模型流程。不同条件的模型采样不锁定，因此差异也可能来自模型产生不同方案/修正，不能把所有查询差都归功于协议。

如果没有发生计划修正，则本批没有足够干预机会检验局部复用，结果应报告为无区分度，而不是宣称等效或证明无用。即使有成本节省，也需检查是否来自提前拒绝或未完成，不能只报总数。

## 记录与预算

入口：`python -m trust_network.demo.run_negotiation --compare-bindings --repeats 2 --out <新目录> --execute`。

最大84次业务模型调用尝试（12×每run最多7），provider格式重试最多168次HTTP尝试；规划约40–60次业务调用、3万–7万token，不是账单上限。owner确认不调用模型。用户已持续授权按需使用MiniMax。

执行前manifest保存源码与案例SHA256、配置模型与处理顺序；每run封存输入快照、签名消息、承诺和执行记录，独立审计重建后评估。旧pilot未封存日志不会被追溯伪装成新审计等级。
