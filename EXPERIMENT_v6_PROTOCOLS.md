# 三协议可靠性主对照：执行前冻结

本批在新的真实供货/物流/买方协商上比较Autonomous、全计划证据、依赖范围证据。先前6/12/3-run试运行与绑定对照作为开发/诊断记录，不混入本批。

固定3案例（economy、urgent、not_approved）×3协议×3重复，共27run；按case/重复轮转协议顺序。模型配置、温度0.2、组织私有状态、公开输入、业务prompt、资源报价校验、验证工具和尝试上限一致。无故障注入，无失败重跑，无中途改配置。

Autonomous拥有同样的verify工具：买方可要求三方确认候选计划，获得结果后决定执行、修正或拒绝。propose明确表示请求执行；该对照在签名和公开格式检查后按Agent决定模拟提交，不强制取得三方承诺。protected执行入口仍强制检查，基线使用独立且明确标注unverified的模拟执行路径，不接真实物流系统。

所有条件共用新增的propose/verify/reject接口；不能将本批直接与旧prompt的pilot比较并归因。两种保护协议的owner检查相同，只有证据绑定粒度不同；所有组复用各自仍有效的缓存，baseline的可选验证使用dependency缓存。不得以禁用baseline工具制造收益。

## 预先规定的结论边界

- H1：保护协议是否降低最终违反供应、运输、买方硬条件的提交率，并保持已授权任务完成。
- H2：发生计划修改时，依赖范围协议能否减少不必要的重新确认。必须补充同草稿重放，区分模型轨迹差异；没有修改机会就报告无区分度。
- 记录错误候选计划、最终错误执行、确认查询、调用/token、正常完成、拒绝/交互上限/失败以及方案质量。候选计划送审不冒充已承诺事实；不能把送审纠错与错误执行混为同一指标。
- 失败run标签null，单列并保留所有尝试成本。事件和结果独立核验后再读取组织私有状态做离线评估；真值绝不回流决策。
- 样本小、合成世界、无固定模型随机种子；不宣称现实零风险或统计显著性。若三组都主动verify且无错误，就报告机制没有足够干预机会，而非筛选错误回答。
- 本批不解决资源真实预留、跨物理设备部署或全部自然语言事实验证。组织责任证据与任何经济/法律归责分开。

每run最多7次业务调用（供应/物流各2次，买方3次），全批最多189次业务尝试、378次provider格式重试后的HTTP尝试。规划约90–120次调用、7万–12万token，不是计费上限。用户已授权按需MiniMax验证。

冻结入口：`python -m trust_network.demo.run_negotiation --compare-protocols --repeats 3 --out <新目录> --execute`。输出manifest保存本规范、源码和案例哈希。
