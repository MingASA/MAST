# 从短路查证转向有范围的修正协议

## 新的真实失败证据

rate_v9_ablation/dependency_single中，物流更新报价后买方明确算出2300≤2400、时间数量均满足，却因旧版反馈current plan not confirmed而拒绝新方案。它还把早到偏好当成整体合并方案的硬限制。此为真实模型轨迹，不是预置行为。不能把拒绝计为可靠性成功，安全完成在该run为0。

问题不是简单地再加一句“请继续”。消息协议没有把否认的作用范围、计划版本和恢复条件结构化交付给接收Agent。程序内部按projection绑定证据，发给Agent的反馈却退化为无版本的泛化文字，丢失了机制信息。

## 下一机制应实现什么

输出机器可验证的repair envelope：rejected_plan_hash、签名否认证据、失效的组织依赖、仍有效的证据、对应的新组织报价及其摘要。否认旧版不自动否认新版；新版只有重新覆盖受影响约束后才能执行。接收网关限制旧版继续下传，允许Agent提出修正版本，不允许Agent把自身解释替代权威事实。

验证策略按依赖阶段批量收集可独立修正的上游冲突，再触发一次Agent修正；暂缓会因上游变化而失效的下游批准。这样与“全查证”以及“首次否认立刻返回”都形成可检验差别。预期收益是降低无用下游批准及逐个暴露冲突的协商轮数；不是保证模型必然理解或产生最优方案。

消融必须保留：现有泛化反馈、只有结构化修正消息、结构化消息加依赖阶段调度。所有组仍使用相同业务Agent提示和执行门禁，不靠给新组更强模型或额外隐藏事实制造收益。跨组织公钥和签名证据复用现有模块。

本文件是根据正在运行的V9消融中已完成失败作出的设计推论。余下运行尚未结束；不提前断言整个批次失败，也不在运行中改写其代码。
