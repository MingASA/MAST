# 在线选择验证与局部阻断

本次直接接入真实 MiniMax negotiation runner。新增 selective 开关；复用既有签名证据、依赖投影和提交门禁，不改模型提示。

机制有两部分：首先按依赖范围找出缺证或失效组织，只查这些组织；随后按预计否认概率/查询成本排序，收到首个签名否认即停止为这一不可执行版本收集其余批准，返回Agent修正。下一版优先检查上次否认的组织，其余未受影响的批准继续复用。完整提交仍必须获得所有必要组织的有效批准，不靠风险概率绕过授权。

概率初值为共同Beta(1,1)，只根据运行中已观察的签名答复更新；目前每次运行重新初始化，未声称已学习可迁移的校准风险。两次检查i、j的预期查询成本分别为c_i+(1-p_i)c_j与c_j+(1-p_j)c_i，因此独立且稳定的否认概率假设下，按p/c降序有意义。此假设不保证在相关、随计划修改变化的真实流程中最优。

这不是把短路求值或贝叶斯均值冒称新发明。研究假设是：结合跨组织声明依赖和否认后的定向重查，可以减少为已经不可执行的候选方案做无用查证。代价是可能只发现一个错误，增加Agent修正轮数及token；必须一起报告。

初次试运行：economy案例，full与selective各1个自然模型运行，温度/提示相同，无注入。对照仅用于接通与发现信号，不用于统计结论。full采用整计划绑定，selective采用依赖绑定加短路，因此后续必须加入dependency-only消融，区分绑定复用与验证顺序各自作用。当前调度优化接在V6流程，不宣称它已与V7资源执行共同集成；动态资源竞争结果不可混入本批收益。
