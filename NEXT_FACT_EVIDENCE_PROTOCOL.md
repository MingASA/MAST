> **实现进展：** [事实补证 v1](FACT_EVIDENCE_V1.md) 已接入真实 worker 的动作门禁并完成16条离线矩阵；本地争议已持久化。以下设计中的跨组织 dispute 广播与自动恢复尚未完成，不与旧 revoke 混用。没有新付费实验。

# 下一阶段：事实补证与冲突约束协议

状态：研究与实现规格，尚未接入运行时，尚未授权新付费实验。依据 checkpoint ee7a12d 的真实中间撤销实验。

## 当前证据的准确含义

root_gate 已让撤销依赖传播到两个下游组织；dependency 对真实模型明确提出的两条转交执行了程序拦截。这是运行时防传播的正向机制案例，但不是配对随机实验或最终错误完成率下降的统计证明。三臂的最终错误账单均为零，任务完成率也偏低，不能只报安全。

push 的两位 middle Agent 在 tick 11 收到撤销，后续 hold 理由明确引用该证据。这是通知后改变行为的观察，不是硬门禁拦截案例。不能因没有故障后 execute 而把 push 样本排除出整个传播控制试验：执行提议本身可能受策略影响，按它筛样会产生选择偏差。三条已实现故障的流程均保留，硬门禁机会与通知后的行为分表。

评分器已补上 claim_refs 的显式索引解析，修复引用计数遗漏；新字段 fault_trial_evaluable、hard_gate_evaluable 区分上述口径。旧 containment_evaluable 保留作为硬门禁机会的兼容字段，不再用作全流程样本纳入条件。holds_after_visible_negative_evidence 仅表示模型输入含相关撤销且选择 hold，不自动推断它由通知造成。

离线复评分见 results/workflow_intermediate02_review/，原始归档不改。当前仓库 integrity 清单84项全部匹配；旧运行报告的104项是历史口径，不据此声称本次校验了104项。15项相关测试通过，无新模型调用。active 校准虽成功完成六个派生阶段和四个中间转交，但最终业务仅完成 1/4；它证明引用接口改善了可执行性，不证明整个业务已稳定。

## 研究问题

freshness 回答的是“发行者是否仍认可这份声明”，不是“事实是否正确”。下一阶段应检测**未撤销的错误事实**，并对冲突只冻结相关依赖。不能用同一个 reliability_status 的 active 回复充当真实性证明，也不能让策略读 truth。

第一版限定为“某费用是否有独立的正式结算依据”，不声称解决任意事实真伪。权威对该具体命题负责；如果权威也错误或串通，单权威协议不保证正确。这一失败边界必须保留。

## 信息和证据设计

1. 来源组织发布 total_charge，仍可签错、漏更新或发布互相冲突的声明。公开金额与其他单据可以完全一致。
2. 买方权威服务单独保管正式结算依据台账，键为 workflow/order/currency，含确认金额、记录版本与有效状态。此台账与授权额度不同：额度足够不能证明应付事实正确。所有臂使用相同台账，只有服务进程读取。
3. 查询具体断言，签名回复 CONFIRMED / CONTRADICTED / UNKNOWN。请求绑定 requester、workflow、claim digest、命题 scope、动作/批次摘要、nonce；回复绑定同一请求、台账版本和有效期。
4. 首版不跨动作缓存，不接受无新 challenge 的旧 affirmative。现有 `authority.py` 的私有服务、Certificate 验签/请求绑定/有效期检查可复用；其既有 formal_model_authorization 语义不得直接改名当成费用真值。扩展新的 scope，保留旧接口和历史结果。
5. 策略只接收签名回复；审计保存原声明、请求、回复与实际动作，评分器独立使用故障真值。证书只证明权威对指定范围作出什么答复。

旧 signed_false fixture 的真值目前在 source 私有状态里，不能不说明就换成买方全知台账。新实验应另命名 `unconfirmed_settlement_basis`，明确它测独立业务确认；保留旧 signed_false 作为尚未解决的通用真实性边界。若要扩展旧案例，必须升场景版本并对所有臂统一改动。

## 程序门禁及局部处理

在现有 run_batch 调用 effect 之前增加独立事实证据检查；与 freshness 为 AND 关系，不互相替代。查询失败/UNKNOWN 产生 REQUEST_EVIDENCE 或 ESCALATE，不能 PASS。模型明确 verify 仍只查证，必须另有 execute。

冲突索引使用公开的规范命题键 `(workflow, predicate, order, currency)`，比较 value。发现同一键不同值时，仅给引用相关声明的依赖链设本地 disputed 状态。CONTRADICTED 也是有证据的 dispute，**不能伪造原发行者的 revoke**。独立证据证明“不足以继续使用”，不自动证明原发行者故意说谎。

解除 dispute 需要权威对精确命题的新有效证据，以及原发行者签发、绑定旧新关系的替代声明。复用 closure v3 的后代重建与任务绑定；新图再次通过 freshness 和事实门禁。若权威无法裁定则继续升级，不投票选一个数，也不自动让模型改金额。相关负面证据沿本地交接反向索引传递时，新增消息类型必须验证证据权威和 scope，不能直接塞进 revoke 接口。

## 可检验的最小算法与公平对照

先不要添加复杂学习器。所有臂共享签名、结构、完整依赖 freshness、模型输入和正常任务服务：

- closure-only：当前完整依赖机制，无强制事实补证。
- conflict-triggered：公开冲突时强制补证；公开一致但隐藏错误是它的失败边界。
- fact-verify-all：每个有相关断言的转交/最终动作均强制补证。
- selective-fact：公开冲突或已知 dispute 时必须补证；最终不可逆动作必须补证；其他转交按本地配置的暴露损失/查询成本阈值选择补证。

阈值只用当前可见信息与配置，不用 case 名、故障时刻或真值。该分数是操作性调度启发式，不是学习出的错误概率或数学 settlement 的等价实现。不可逆动作强制补证是安全底线；少查转交可能让错误先传播，必须同时报传播距离与最终动作，不能靠零最终错误掩盖传播代价。

控制条件至少有：正常已确认；公开一致但未获确认；公开冲突且权威可裁定；权威 UNKNOWN/不可用；过期/错请求回复；无关订单继续。另保留权威自身错误的边界，不能配置服务总是知道真值后声称普遍真实性保证。

## 执行顺序与验收

1. 实现窄 scope 的签名服务与 effect 前强制门禁，证明可阻止明确准备执行的提议；先做少量固定提议的离线矩阵，不调用模型。
2. 增加本地 dispute 与精确解除条件，复用依赖重建和签名审计；未知不放行、错误 scope 不影响无关订单。
3. 保存各臂同样的提议、证据包、权威台账和延迟；不为各臂重新生成不同真值。
4. 报告事实补证次数、freshness 次数、总网络消息、时延、任务完成/升级、错误传播、最终 unsafe、dispute 范围和证据路径。来源身份与事实错误责任分开。
5. 离线结果能明确展示不同检查覆盖的收益与失败边界后，再交接小规模 live pilot。先不要继续扩大撤销矩阵，也不直接付费跑尚未实现的事实补证策略。

研究增量应由“过去只能在撤销后阻断，现在在未撤销但有独立矛盾证据时也能程序阻断，并限制影响范围”来支撑，而不是把新场景上的全查当成新算法首创。
