# 最终目标与执行路线

用户授权：持续推进直至达成目标，关键研究取舍由用户决定。用户随后明确授权按需要随时调用MiniMax做真实验证，不必过度节约；常规实验不再反复询问费用授权，仍记录用量和结果。环境沙箱的网络权限是独立限制。

要交付的是跨组织Agent协作的证据约束协议及可运行demo：Agent承担理解、判断和协商，组织网关约束消息与动作，接收方根据自己的可见信息和已验证证据决定继续、补证、局部修正或升级。第一目标是减少错误信息传播和错误行为，第二目标是检测、定位和可核验责任记录。

不预先承诺某个Risk-Aware公式一定获胜。不以删掉失败案例、让所有任务升级或给对照组弱化工具制造收益。旧四世界及其结果作为历史和回归保留。

## 最终验收

- 正确性：同时测最终不安全动作和错误声明跨组织传播；保留未授权、错误转述、过期/冲突证据和正常案例。
- 可用性：正常业务完成率不能因“全部阻断”虚高可靠性；局部可修正草稿先在本组织修正，避免无必要升级。
- 费用：报告真实证据查询、修正次数、升级、API/token。强制验证使用相同缓存和工具；不把token与模拟成本冒充同一种货币。
- 机制证据：展示真实LLM准备发送的错误声明/动作被程序阻止或修正；配对回放只证明同一提议下的协议差异，完整流程另测。
- 组织边界：发送组织本地签完整提议，接收方仅用公钥验收；runtime不代持全部组织身份。权威事实不流出组织，只有范围明确的证据。
- 可追溯：独立重放证据与动作状态、核对签名和最终指标；责任结算的模型假设单列，不冒称通用因果归因。
- 证据强度：离线反例/故障注入、真实LLM流程和现实外部有效性分开报告；只有真实批次出现稳定增益，才称demo达成目标。

## 当前决策

型号正式批准作为发运的硬前提，不以低风险先验免除。选择性优化放在证据复用、验证范围、局部修正和非硬性辅助检查上；不再为了省一次查询而绕过硬前提。保持一阶段可解释策略作为起点，无需立即接入全部DP/game。

现有三角色案例仍用于基础验收。下一版需增加各组织不同且必要的业务判断/协商内容；协议只证明有限关键声明与动作，不宣称证明任意自然语言的真假。

## 进度

已完成：修复过期否认被遗忘、查询nonce跨run重复；新结果增加完整结果签名，兼容读取历史审计；向业务模型显式提供证据验签状态。13项相关回归通过（排除HTTP测试）。这只修复已确认漏洞，不代表独立审计状态机已全部完成。

已实现协议基础：完整提议封包签名、接收方输入/nonce/身份校验、按组织操作权限检查intent、结构化claims与证据范围绑定、先修正再传播。自由文本保留为原始草稿记录，不直接提升为可执行声明。

已接入组织子进程网关：worker读取组织本地私钥，runtime只持业务组织公钥和自身审计密钥；业务提议及最终可传播封包分别由组织签署。下一组织网关在调用模型之前独立验收上游封包。组织网关再次确认权限与证据后才签署最终封包，最终执行要求三组织收据。

新增独立审计器重建证据状态、提议/收据绑定、角色顺序、提交和指标。12个无模型调用的进程工作流验收见results/contract_v5_preflight_checked/report.md，覆盖正常、未授权、错误数量草稿和越权操作草稿。它们是显式脚本故障注入，不是MiniMax收益，也不提供OS或真实跨设备隔离证明。71项测试通过（7.44秒），1项HTTP测试未执行。

新增收据链约束：接收方拒绝缺失、重复、乱序及跨workflow收据；组织最终签发同时绑定前序收据哈希。HTTP组织网关及本机四服务验收已通过，见results/contract_v5_network_retry/manifest.json；这是网络适配验收，不是不同物理设备部署。

真实网关pilot已完成6个MiniMax run，18次已记录API调用、20560 token。三组均完成已授权订单、主动停止未授权订单，未出现程序修正；它验证了真实交互，未展示增量收益。详细结论见results/contract_v5_minimax_pilot/interpretation.md。不能以这一试运行标记最终目标完成。

待完成：补足具有人工判断空间的案例（见SCENARIO_v6.md）、跨设备验收和真实Agent对照。新协议主实验需区分自然模型错误与公开声明的故障注入；后者验证面对实际网络错误时的机制作用，不能冒称模型自然错误率。按用户最新授权直接开展必要MiniMax验证，记录用量。实现问题和研究负结果持续公开，不提前标记目标达成。

第六轮已实现多条件交付计划、按组织依赖范围的承诺、旧否认保留、局部失效与SQLite幂等执行。3个真实协商案例完成可行性检查，10调用/8203 token，出现1条非注入的报价错误被程序拒绝后修正的轨迹；尚无同期对照且业务偏好质量不佳。结果见results/negotiation_v6_pilot/report.md，持续反思见RESEARCH_LOG.md。下一步补齐该新流程审计封存并冻结对照设计，而非继续堆成功示例。

已补齐协商输入快照和完整结果签名、独立审计重建。12个绑定对照和27个三协议主对照已完成；后者Autonomous不安全1/9，保护组均0/9，依赖范围组已授权安全完成6/6，但保护组未收到错误propose，尚无稳定总体增量证明。全部轨迹可查看results/negotiation_v6_protocol_comparison/demo.html。

剩余关键缺口：真实动态资源承诺/跨订单竞争、主张与执行的进一步故障验证、跨设备部署和责任证据交付。Docker在当前环境探测时不可用（WSL集成提示），不能沿用旧环境成功记录声称已经完成隔离部署。新的认证HTTP服务入口已准备，但远程主机验收尚未完成。目标仍保持active。
