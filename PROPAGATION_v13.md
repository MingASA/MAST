# 依赖消息通道：传播安全与接收责任

claim_channel.py复用现有Certificate签名，新增组织本地ClaimGateway。声明内容、工作流和父声明摘要均被签署；按预先约定的predicate确定事实权威。当前仅支持精确转述，内容被改写则阻断，不把任意LLM推理结果自动升级为权威事实。

接收网关要求父证据齐备。权威撤销需携带原声明并证明同一签发者；可先收到撤销再收到旧声明，旧声明及依赖它的转述仍不能使用。execute在实际调用effect之前重新检查祖先依赖；受影响分支被阻断，无关声明继续使用。接收、撤销接收、阻断和使用记录由本组织签名并以摘要链接。

针对性测试覆盖权威→中介→执行方依赖、篡改金额拒绝、撤销传播、先撤销后声明乱序、阻断实际effect且不影响无关分支，1项测试通过。

这只是协议基础组件，不是已完成的真实网络实验。当前在内存中存储，无跨进程通知交付，未实现TTL、撤销送达时限或原子化并发执行；失联组织尚未收到撤销时不能保证即时阻断。日志中的use_allowed表示检查允许，不证明外部动作成功。下一步应把受控撤销和真实Agent转述接到多个组织服务，测错误到达跳数及撤销前后使用情况，而不是声称本地测试已经证明跨设备安全。

追责范围：签名撤销接收记录可以证明某网关已经知道撤销；缺少接收记录不能推断它已知。正确转述、擅自修改、撤销未送达、收到撤销后仍执行应分开评价。没有约定通知义务和时间界限，不将网络延迟自动解释为组织失职。

## 真实多进程实验接入

claim_worker提供组织本地持久消息状态、MiniMax交接决策和执行门禁；两个组织依次产生交接建议及相同事实的签名派生声明。run_claim_propagation将权威→采购协调→仓储串起来，实验环境先只向协调方送达撤销，再向仓储送达，分别测量旧依赖是否仍可执行，并检查独立订单不受影响。

这是刻意保留的通知延迟反例：只靠撤销通知的方案在通知未到时仍可能执行旧授权。不能把“收到后阻断”写成“撤销立即全网生效”。本地serial子进程和JSON状态不代表跨设备并发可靠部署。交接建议不作为已确认事实使用，仅严格转述的结构化fact可进入当前通道；业务语义复杂推理仍未覆盖。

## 基于送达证据的追溯与主动查询入口

## 从转述扩展到有完整性要求的派生结论

新增claim_derivation.py的sum_charges规则：总价必须依赖签名费用清单（charge_manifest）和清单要求的全部分项证据。清单由配置中的权威签发，各费用谓词也各自配置权威；网关只根据公开证据计算。金额用整数分，订单和币种必须一致，漏项、重复项、混入其他订单或币种、错误总价均不接收。派生总价继续绑定所有父摘要，因此任一来源撤销都会影响总价和其下游转述。

此设计避免“已披露数字相加正确”被误当成“费用完整”。清单权威若自身漏列必要项，仍需追溯其清单，不假装接收方能看穿私有状态。2项针对性测试通过；这次扩展尚未由真实Agent生成总价并通过网络转发，不能追认为旧转述实验已覆盖它。

算术检查不是创新声明。更有研究价值的是把声明完整性、可验证转换、上游撤销和下游可用性接在同一个依赖协议中，并使错误来源与错误转换的责任可区分。

## 动作合约不等于声明有效性

新增claim_action_contract.py并接入claim_worker的approve_invoice操作。接收方要求同一订单的总费用与明确账单批准授权，核对动作类型、币种和金额上限，并检查所有声明祖先的撤销状态；仅有正确总价、其他订单的授权或已撤销授权均不能触发模拟批准。允许、阻断和effect返回摘要留在本组织签名链。

这只授权“确认账单”，不授权付款或出库。实际出库还必须接入资源承诺及特定出库授权，不能以更换操作名称假装已完成。当前测试覆盖缺授权、跨订单、撤销和正常批准；尚未做该动作合约的真实模型流程实验。effect返回事件证明组织记录了返回值，不是外部付款完成凭证，也未提供跨进程并发原子性或持久业务幂等性。

audit_claim_propagation已对真实pilot验证各组织签名事件链和序号，重建仓储本地第5次事件允许旧授权使用、第6次收到撤销、第7次阻断同一依赖、第8次允许无关订单。该顺序不依赖跨机时钟。不能仅凭其他组织已收到撤销而判定仓储此前知情；签名use_allowed也不是物理发货完成证明。

新增execute_checked：执行前给根声明权威发送随机challenge，答复必须签名绑定工作流、根声明、派生声明、操作及本次challenge；缺答复、错范围或非active均不执行。针对性测试表明，即便本地未收到撤销，权威确认已撤销时也会阻断。该入口尚未连入真实服务实验；查询与最终物理执行之间仍存在状态变化窗口，不能宣称原子授权或绝对即时撤销。
