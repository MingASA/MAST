# 下一阶段收敛：检验选择性验证，而非继续扩展事务系统

## 目前证据支持什么

V6真实27-run中自主组出现1次不安全执行，保护组没有；但保护组未收到同类错误提议，采样差异仍存在。固定真实轨迹重放支持依赖范围复用减少部分查询，真实完整运行的token收益尚未稳定出现。V7证明静态事实证书不防止跨订单超分配；资源预留能补缺口，却有确定的协调开销。最新真实资源流程通过签名输入、授权、提交确认独立审计，但只有1个样本。

因此目前不能声称“风险算法显著优于全验证”或“新型分布式事务算法”。已有价值在可执行门禁、证据范围绑定及错误恢复；成本优势仍是待检验假设。

## 下一实验必须固定的条件

采用同一业务约束、执行端资源保护、Agent提示和模型参数。Autonomous仍保留可选查证工具，系统公共硬授权不能只在一组缺失后被包装为风险预测收益。业务执行API原生提供的幂等性和资源扣减作为共同基础；静态无资源账本反例单独保留。

全验证与选择性组的差别仅为重查哪些受影响的声明。全验证也允许完全相同计划的有效证据复用；选择性组仅复用依赖范围未变、且语义允许复用的证据。库存可用性快照不等于排他承诺，不得因为价格没变就复用已经消费的资源。

先固定真实模型提出的修正轨迹作机制重放，报告每条证据为何失效、为何复用；随后用相同随机化案例进行自然Agent完整流程对照。受控报价变化、部分信息遗漏和自然模型错误分开标记，不把注入错误率充当真实世界发生率。

## 可否证条件

若变化几乎总影响全部组织、复用节省被协商token抵消、或减少查询导致安全完成下降，就不能接受成本可靠性双收益结论。需同时报告无干预机会的样本，不只展示修正成功的轨迹。

V4风险先验是人工指定，尚未经独立数据校准。若选择性来自可验证依赖关系，应使用准确名称“依赖范围证据复用”，不要将其改称学习到的风险策略。omission operational approximation与数学结算不能混同；新增责任项的增量必须另做消融。

当前不再扩大资源协议功能范围，除非真实对照被具体正确性漏洞阻断。下一交付应是一份预注册的统一比较和可重复脚本，而不是继续堆积孤立的协议验收。
