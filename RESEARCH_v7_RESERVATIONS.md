# 动态资源：从事实查证到可兑现承诺

## 已确认的问题

V6 的 owner_check 只判断单份计划是否满足本组织静态记录。两个不同订单各自采购同一批 early=4、later=6 库存，或占用相同 air/road 运力，两份计划都会通过。即使每份都有签名、有效期、正确绑定以及执行端幂等性，也不能防止跨订单超卖。签名证明检查发生过，不会自动保留资源。

这不是新的自然 MiniMax 错误统计，而是明确构造的并发协议反例。原有 27-run 的结论仍限定于静态状态。

## 本次实现与验证

新增 trust_network/demo/reservations.py。每个供应方或运输方独占本地 SQLite，BEGIN IMMEDIATE 将容量检查与预留写入放在同一事务内。预留绑定组织依赖范围和交易；无关的费用修改不消耗第二份供应预留。同一交易的不同有效范围拒绝覆盖，必须先释放或等待到期。

held 可到期或主动释放；committed 永久占用，不能靠证书到期重新出售。重复提交返回既有状态；已提交资源的撤销需要独立补偿流程。重启时验证资源配置摘要，不允许换一个容量快照就悄悄改变已存在的资源账本。

验证命令：`.venv/bin/python -m pytest trust_network/tests/test_reservations.py -q`，3 passed。覆盖供应/运力并发争抢、重启后重复提交、已执行容量不随 TTL 释放、过期禁止执行，以及范围复用。当前没有调用模型。

## 尚未完成的运行时部分

该模块目前是本地存储基础组件，尚未接入 V6 workflow；不能把上述测试称为跨组织完整协议收益。下一步需要：

1. 由组织网关签发独立的 reservation 证据，区分“检查通过”和“资源预留”；旧 commitment 不得冒充预留。
2. 协调方收齐预留后持久化提交决定，组织执行端验证决定及预留范围，再改变本地状态。协调方不得读取他方资源账本。
3. 明确部分失败语义：准备失败可释放；已提交不能假装原子回滚。必须记录 pending/recovery，并通过重试或业务补偿收敛。尤其不能让一方已提交、另一方预留到期时仍报告整体完成。
4. 公平固定并发输入、容量和状态变化，比较静态查证与资源协议的超分配、安全完成、等待/拒绝、网络次数与恢复成本。受控调度与自然模型输出分别报告。

先验证上述协议闭环，再运行真实模型动态实验。若所有失败仅变成永久挂起，也不能算达成稳定性目标。

## 后续实现：持久化决定与恢复基础

新增 resource_protocol.py：组织服务将 held 提升为不自动过期的 prepared，签发仅覆盖本组织依赖范围的准备票据。协调组件仅持有公钥、票据及自己的签名密钥，检查两方准备票据和当前买方批准证据后，先持久化签名决定，再允许分发执行。组织签发执行确认；协调端收齐确认前只能报告 recovery_pending。重复提交/终止可重放，已存在的决定不可更改。

新增三项协议测试，与本地预留三项一起运行，6 passed：覆盖部分提交后模拟失联、超过原预留时间后恢复、协调组件重启、准备状态不自动释放、显式终止回收资源、重复终止、缺票和伪造决定拒绝。测试仍为进程内服务方法调用和受控故障时序，并非已完成真实跨机器或 MiniMax 动态实验。

这采用经典两阶段提交的阻塞取舍，不作为新算法贡献。信任假设是单个诚实且持久化可靠的决定服务；尚不防止该服务恶意签发相互矛盾的决定，不提供拜占庭共识。prepared 在服务故障期间可能长期占用资源，应将等待及占用时长纳入成本，而不能只统计正常路径网络次数。

当前进度替代上文的“尚未完成”中第1—3项的部分基础工作，但仍未接入真实 Agent runner/HTTP 服务。部分准备失败的自动终止编排、服务部署、独立审计重放和真实实验还未完成。静态 V6 实验不受这次改动影响，也不被追认为具有动态预留保障。

## HTTP 组织接口验收

negotiation_worker 新增 resource_prepare/resource_decide，由组织服务读取自己的私有状态、签名密钥及资源账本。negotiation_http 的已有 bearer 认证与远程 TLS 要求适用于这两个操作。资源操作不调用模型，也不接受客户端传入的私有容量或当前时间。

运行 `.venv/bin/python -m trust_network.demo.resource_network_smoke --out results/resource_v7_network_smoke` 成功：9 次真实 HTTP 请求、0 模型调用。供应与运输服务运行在独立本机进程，验证准备、部分终止时保持 recovery_pending、重新创建协调组件后恢复、重复终止和资源重新使用。完整请求/响应与公钥保存于结果 report.json，服务 token 和私钥未导出。

这一验收是受控的终止/恢复路径，未伪造买方 Agent 的执行意愿，也没有将它报告为真实 LLM 提交实验。网络提交路径仍需经真实买方计划及批准证据接入；尚未完成不同物理机器部署、自动故障恢复调度、一般化独立审计。测试结束已终止临时服务，并释放最后一次探测所占资源。

## Agent 执行授权与编排入口

提交门禁增加必要条件：买方签名消息必须是 propose 且完整计划摘要一致；仅有批准证据不够，因为旧 attest 接口允许针对 verify 候选计划签发批准，而 verify 不代表愿意执行。该修复避免把“请求查证”升级为“请求执行”。

新增 execute_resource_plan 运行时编排入口：校验买方执行意愿，调用买方批准接口，逐组织准备资源，全部通过则持久化提交；一方拒绝则持久化终止，并释放已经准备的其他方。终止完成按实际已准备集合核对，不再等待从未准备的组织确认。传输异常传播给调用方，不能生成成功结果；决定前丢包导致准备结果不明时，仍需要后续恢复/查询协议。

协议测试现为5项，全部通过，新增正常执行和运输容量已占用时供应预留释放两条编排路径。这里只以测试生成买方签名，尚未运行本轮真实模型，不将它计入 MiniMax 实验。下一步将真实模型工作流的执行分支接到该入口，并保留部分失败日志。

## 首条真实模型 HTTP 提交已完成

resource_network_smoke 增加显式 --env-file 真实模式：供应和运输 Agent 产生报价，买方 Agent 自行选择 propose/verify/reject；propose 接到 execute_resource_plan，执行组织本地持久化预留和提交。真实调用仍在独立本机 HTTP 进程中，不是不同物理机器。

首试因调用点误传浮点时间而中断，3模型调用/2541 token，未发生资源准备或提交，原始失败留在 results/resource_v7_minimax_pilot。修复后独立重跑 results/resource_v7_minimax_pilot_retry 成功，3模型调用/2528 token、8 HTTP请求。两次合计6模型调用/5069 token，不隐藏首试成本。成功轨迹签署了完整事件摘要；事后读取三方私有状态检查最终计划，三方均无硬约束违反。

这是端到端接通证据，尚无并发对照，不能据此声称预留协议带来统计增益。剩余重点为同容量跨订单对照、准备响应丢失的恢复、独立审计以及公平成本统计。临时服务关闭后资源账本不作为可恢复生产部署保留；这仍是实验生命周期。
