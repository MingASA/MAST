# 第四轮整体核对：理念、算法、实现与实验

核对日期：2026-09-07。结论：研究方向合理，执行前程序门禁已真实接入，但目前是可信中央调度器下的单一授权事实原型，距离跨组织网络中抑制错误信息与错误行为传播的机制仍有明显缺口。发现了可复现代码问题，不建议直接扩大 MiniMax 实验。

本次阅读了第四轮 policy、authority、worker、runtime、场景生成、评估、审计、责任适配、实验脚本，以及既有 graph、simulator、global DP、attribution、settlement 和 game 的主要路径。执行61项已有测试全部通过（7.03秒）；当前受限环境下主动排除1项HTTP服务测试，没有将其记为通过。另用少量离线探针复现缺陷，不调用模型、不修改历史实验或生产执行逻辑。数学侧核对不等于对所有输入的形式化证明。

## 需要优先修复的实现问题

### P1：过期证据会抹掉已知否认，重新允许未授权交易

位置：[approval_runtime.py:62](trust_network/demo/approval_runtime.py#L62)、[reliability.py:68](trust_network/demo/reliability.py#L68)。

evidence_status 将过期证据直接清空为 missing；policy 随即用类别先验替代已知事实。离线复现：低风险未授权订单先取得真实签名的 denied，时钟越过有效期，Agent 随后提议 pass，Risk-Aware 最终提交成功，整个流程仅查证一次。

这是证据状态建模错误。证据过期意味着不能继续据此确认当前批准状态，不意味着过去观察到的否认从未发生。应保存证据历史与当前可用性，至少区分 never_observed、fresh_approved、fresh_denied、stale_approved、stale_denied、conflicting；历史否认须由更新的权威证据解除，或升级处理。不能静默回到低风险先验。

本次36个真实run中没有 evidence_invalidated 事件，因此该反例不推翻已报告的计数，但证明实现存在未覆盖的漏放路径。

### P1：gate 不约束要传播的业务声明

位置：[reliability.py:30](trust_network/demo/reliability.py#L30)、[approval_runtime.py:127](trust_network/demo/approval_runtime.py#L127)。

VisibleContext 只含受理类别、证据状态和预算，不含动作参数、声明及其证据依赖。只要型号批准状态满足要求，Agent 的 findings 和 public_message 就可以继续传播。离线复现：公开数量10，Agent 声称“数量999已经独立批准”，Verify-All仍把这句话转交给下游，并完成原始订单提交。

这个反例证明错误信息可传播，不表示实际发运数量已变为999：当前执行记录仍绑定原始bundle。现有unsafe指标只测未授权提交，也不会统计这类错误信息传播。

应先将本demo支持的关键声明/动作结构化，例如型号批准、数量、订单版本、付款许可、发运许可，声明引用确切证据与适用范围。程序可检验字段一致性、权限和证据覆盖；无需承诺自动判定所有自然语言真假。无法验证的解释可以保留为组织意见，但不能自动获得可执行授权地位。

### P1：组织身份与消息签名尚未形成独立信任边界

位置：[run_reliability.py:35](trust_network/demo/run_reliability.py#L35)、[approval_runtime.py:142](trust_network/demo/approval_runtime.py#L142)、[documents.py:38](trust_network/demo/documents.py#L38)。

runtime生成并持有全部业务组织私钥，替模型响应签发组织证书；远程worker返回的普通JSON也由runtime代签。Certificate不包含public_message或完整Decision摘要，下游没有独立的程序验收入口。当前签名能支持“可信调度器记下了这些输出”，不能作为组织分别确认/签发这些动作的独立证明。

应让组织本地网关持有自身密钥、签署完整动作封包与证据引用，接收方校验身份、请求关联、权限、版本与签名；执行服务只接受通过本地门禁的动作。可以继续使用中央编排器，无需为了分布式而先引入共识系统，但不能让编排器代持所有组织身份而声称独立追责。authority的远程公钥适配已有可复用基础。

### P1：审计通过并不保证统计结果可信或完整重放

位置：[approval_evaluation.py:24](trust_network/demo/approval_evaluation.py#L24)、[approval_runtime.py:151](trust_network/demo/approval_runtime.py#L151)。

审计root确实有签名；但runtime.json中的policy、status、outcome、tokens、verification_count等未被该签名覆盖，也没有全部从事件重建核对。离线探针保持原始签名和审计链不动，仅修改上述字段，validate_audit仍通过。status参与有效run分母，policy参与分组，这不仅影响展示。

验证器还只是根据事件中自报的context重算gate，没有从证据事件重建evidence_status、nonce和时间有效性，也未完整验证“提议→门禁→转交→三个组织依次验收→提交”的状态机。每条事件没有统一可信时间记录，历史有效期独立核验也不充分。

应从事件重建状态、证据与计数并校验结果，签署最终结果摘要；对gate验证依据而非只验证算式。公开消息也应进入其发送记录/消息签名。可信外部保存的公钥和审计根仍是前提。这里发现的是验证能力缺口，没有证据表明现有实验结果被篡改。

### P2：补证交互无法解释已经完成的程序检查

位置：[approval_runtime.py:71](trust_network/demo/approval_runtime.py#L71)、[approval_runtime.py:94](trust_network/demo/approval_runtime.py#L94)。

runtime已验签，但只给模型原始证书，未明确提供机器验证状态、查询结果/缓存命中回执。相同请求命中缓存后，模型基本重看原始输入，两次响应上限使重复请求升级。真实批次中3个已授权run因此升级。

应增加结构化证据视图与工具回执，将复用已验证证据和重新获取当前证据区分为明确操作，保留预算和重复请求限制，而非简单增加重试次数。

### P2：请求标识并不保证跨run唯一

位置：[run_reliability.py:57](trust_network/demo/run_reliability.py#L57)、[approval_runtime.py:81](trust_network/demo/approval_runtime.py#L81)。

同case和repeat的三个policy共用request_id，batch共用authority公钥；底层nonce只是request_id加查询计数。相同bundle的证据在有效期内可能跨这些run通过绑定检查。当前没有跨run共享缓存或已知重放事件，但“防跨run重放”的能力不应由现有单项测试推出。

应为每次工作流和查询生成独立不可预测标识，签入作用域及接收方/操作绑定。若业务允许证据跨run复用，应显式定义可复用授权与查询回执的区别，而非偶然复用nonce。

### P2：权威数据模型与错误日志过于简化

authority registry只有交易、型号和批准布尔值，没有批准记录版本、撤销序列等。把当前请求bundle哈希签进去，只证明响应绑定该请求，不等于登记簿曾批准该bundle所有字段或版本。当前证据明确只覆盖型号授权，后续实现必须保持这一范围，不能扩大解释。

worker失败被压缩为RuntimeError，usage随异常丢失；真实批次两个失败无法定位。应保存脱敏错误阶段/状态、尝试计数和已知用量，未知值明确为unknown。当前报告已披露此限制。

## 核心理念与算法设计

### 可以保留的部分

- LLM提议动作，程序控制转交和提交，避免仅靠提示词自律。这条执行路径已实现，Verify-All真实阻断提供了证据。
- 正式事实留在其他组织，通过限定范围的证据回答；online policy不读取ground truth。现有代码与公开同视图测试支持这点，但本机同用户子进程仍不是OS隔离。
- 查证、升级、残余风险存在成本取舍，应保留低风险漏放反例；追责可作为补充，不能替代预防。
- 贝叶斯更新、签名原语、数学侧归因/结算模块可以继续复用。所读主要路径和本次测试未发现需要推翻它们的基础算式错误。

### 必须收窄或补齐的部分

**1. 当前不是网络动态策略，而是两档固定风险的一阶段门禁。**

理想权威假设下，查证费用为c+pH，放行费用为pL，可再加责任偏好项；在查出问题后升级且不发生自动下游损失的假设下，这个算式自洽。默认高风险p=.2、L=100：放行成本20、查证成本4，且p超过硬上限.05；低风险p=.02、L=10：放行成本.2、查证成本3.1。于是无证据时基本等于“变更单查、普通单放”。

每个组织用同一套参数，没有各自的动作损失、可选证据类型、信息质量、后续路径或局部责任成本；除了证据状态和预算，并无动态信息更新。复用bayes不意味着复用了全图DP最优策略，也未把game激励机制接入真实组织行为。这是明确的简化，不应包装成一般多Agent网络最优算法。

**2. 安全边界和可交易风险需要分开。**

如果研究场景把“正式批准”规定为不可缺少的执行前置条件，则低风险时免除这项证据要求，与该业务约束冲突。如果允许承担小概率未授权风险，则应明确这是受风险预算约束的研究决策。不能同时宣称绝不允许无授权执行，又通过不检查同一授权事实来证明节约成本。

建议先定义硬性动作约束，再在允许承担残余风险的补充检查上做成本选择；也可以利用已有有效证据减少付费查证。若所有任务只有一个未知的硬性批准事实、一次完美查证工具，没有已有证据，选择性查证的空间本来就有限。

**3. 优化目标与主指标未完全一致。**

policy最小化预计损失/费用，实验主指标对所有不安全完成等权；先验20%/2%与压力矩阵50%错误比例也不同。这不是公式错误，但不能用成本最优推导不安全率也接近Verify-All。升级成本H进入决策却没有汇入现有postmortem social（只计查证和实际损失）；因此也没有按policy完整目标评价结果。token另列且不能直接与模拟业务成本相加。

宜采用明确的目标：在不安全率和正常完成率约束下比较验证成本，或同时报告完整费用/损失权衡；风险估计给出来源与不确定范围。硬约束或保守风险上界必须根据任务定义，而非事后为了结果调阈值。

**4. 遗漏责任尚未形成真实激励闭环。**

当前可选w*p*delta*L与已文档化的operational approximation一致，但不取决于实际组织责任份额、可追责概率和后验归因，不能解释成真实预期赔付。主实验w=0；postmortem又固定单一seller起源和理想终局发现，无法识别新增的模型错误、转述失真、隐瞒或多源共责。它是合成结算适配，不是一般事后定位算法已在demo得到验证。

**5. 需要声明信任假设，而非笼统说“跨组织就没有信任”。**

当前依赖可信runtime、预置公钥、准确且不说谎的authority、可用登记簿和时钟。跨组织信息不可见并不要求删除所有信任根；关键是明确哪些主体/陈述可信、可信到什么范围、失效如何处理。当前工作首先可以覆盖错误/疏漏Agent，不能延伸成对恶意组织、错误权威和执行方绕过门禁的全面保障。

## Demo与实验能否验证核心理念

公开单据不泄露真假这一要求满足；但三个业务档案各只有一句职责，三个角色基本都围绕同一布尔事实决定pass/request/reject，权威端是确定性登记查询。这个流程适合检验授权门禁，却不足以体现丰富的人工判断、跨组织独有知识或多个错误产生与传播环节。去掉部分中间角色，核心授权判断仍相同。

后续应给各组织不同且必要的业务任务和有限声明，例如变更适用范围、验收条件、履约条件的解释与协商；限定程序验证的动作前提，不把所有人工工作改写成逐字段校验，也不刻意迫使LLM犯错。保留现在的四世界作为门禁单元案例，增加流程深度应由研究假设驱动。

真实结果仍是Autonomous 1/12、Verify-All 0/11、Risk-Aware 0/11，两个失败单列。Risk-Aware无非allow干预，不能证明其增量预防效果。匹配初始输入相同是必要条件，但每次模型采样不同，后续输入受干预影响；本批样本小且只覆盖四世界，不能由个别差异认定因果。基线是本轮共用新prompt的Autonomous，不是旧轮原封不动的运行条件。

error_blocked当前表示“真值未授权且没有最终提交”，包括拒绝和升级，不等于“识别出错误”。已授权误阻断的统计是必要的；后续还应分开确认错误后阻断、证据不足暂停、运行失败，并补充错误声明传播指标，避免仅保护最后一步而忽略中间误导。

## 修订顺序与验收标准

1. **先修证据状态、消息/动作范围、审计结果一致性。** 以本次反例为回归标准：旧否认证据到期不能自动洗掉风险；不支持的数量声明不能被提升为可信执行依据；修改结果分组/状态/计数必须被检测。
2. **补齐组织签发和接收方程序验收。** 组织密钥留本地，签完整动作封包；共享runtime可编排，但不能伪装组织独立承诺。至少一个真正远程组织参与可验证的签发/接收闭环，再声称跨设备机制验收。
3. **明确硬性动作前提与选择性检查空间。** 扩展为按声明、操作和现有证据做决策，先保留一阶段可解释policy，无须立刻迁移全部DP/game。
4. **修回执与日志，再做已有真实提议的离线配对回放。** 区分同一提议下policy差异和完整LLM流程差异；只有这些验收通过后再规划小规模真实批次。

本次没有修改runtime、算法参数或历史结果，仅新增本报告与可复现审查探针。探针入口：`.venv/bin/python -m trust_network.demo.review_reliability`；结果见[probes.json](results/reliability_v4_review/probes.json)。它故意确认当前缺陷，修复后应转为“反例不再成立”的回归测试。
