# 实验执行任务书：跨组织 Agent 网络的在线可靠性机制

版本日期：2026-09-09。本文供**只能看到仓库、没有历史对话的实验执行代理**使用，可作为你的主任务说明。阅读后应实际完成实验接入、运行、分析与报告，不要只做代码审查或提供实验建议。

## 1. 研究问题与用户意图

我们研究不同组织、不同设备上的 Agent 合作完成原本需要人工协作的工作流程。各组织的数据库、内部批准记录和工作手册不互相公开。一个组织发送的单据可能表面合理，但关键事实只有另一组织知道。

最终目标，按重要性排序：

1. **阻止错误信息继续传播、阻止错误动作发生，提高整个协作网络的安全性。**
2. **错误发生后，依据可核验证据尽可能合理地检测、定位和追溯。**
3. 在前两项成立的基础上，保持正常任务完成能力，控制验证、通信及模型成本。

不是只做数学建模，也不是只写prompt让LLM自觉查证。机制必须进入运行时，能够阻止一个LLM已经准备放行的结果。不能靠全部拒绝获得表面安全。成本不是唯一或首要研究目标。

用户要求上一位代理先集中实现，再由你执行真实实验，以节约开发代理额度。**你是接手实验工作的代理，业务流程中的Agent仍应通过真实模型API产生决策。**“换一个代理执行实验”不等于授权你随意更换实验中的业务模型。默认复用仓库环境中配置的MiniMax；如果用户另外指定模型，再更改并单独记录。

## 2. 工作约束与权限

- 当前环境历史工作目录为`/home/cjy/wuxing`，但应以你实际拿到的仓库根目录为准。
- 使用`.venv/bin/python`。若环境不存在，按`pyproject.toml`或锁定依赖准备环境；不要假装已运行。
- 用户此前允许为研究调用MiniMax，但本阶段由你接手执行；先完成下面的小规模pilot。新增大规模付费实验或改变主要研究方向前，让用户决定，不要无限重复试验寻找正结果。
- 配置候选路径是`/home/cjy/cyberagent/.env`，更早环境曾为`/cyberagent/.env`。只检查实际存在的配置；若都不存在，向用户索取路径。不要在输出、日志、仓库或报告中打印/复制密钥。
- 复用`trust_network/demo/provider.py`：读取`CAI_MODEL`、`OPENAI_BASE_URL`或`OPENAI_API_BASE`、`OPENAI_API_KEY`。实际模型名由配置决定，不凭本文假定。提供商兼容接口使用这些变量不代表业务模型是OpenAI。
- 不要为了完成本任务推送Git、部署公开服务或安装一大套新架构。允许为实验补必要的适配器和多轮调度代码。
- 减少无关测试、反复探测和零散功能扩张。先围绕核心机制产生可解释的真实证据。

## 3. 必读文件与代码地图

按以下顺序读，不需要从头读完全部历史任务书：

1. `SAFETY_AND_ACCOUNTABILITY.md`：研究优先级和追溯边界。
2. `RELIABILITY_HANDOFF_v20.md`：当前接口、命令、补证恢复、最近修复和限制。文件后部包含后续补充，应读完整。
3. `EXPERIMENT_OPERATOR_v20.md`：前一版实验交接与历史preflight说明。涉及源码版本时，以当前代码和新生成manifest为准。
4. 本任务需要的源码：

|模块|作用|
|---|---|
|`demo/claim_channel.py`|签名声明、依赖、撤销、组织本地事件链|
|`demo/claim_derivation.py`|精确转述和带完整费用清单的总价派生检查|
|`demo/claim_action_contract.py`|账单批准合约：正确总价不等于有动作授权|
|`demo/network_reliability.py`|批次规划、根证据共享查证、门禁、局部冻结、签名报告|
|`demo/reliability_recovery.py`|权威补证关联新旧声明、派生重建范围、恢复信封|
|`demo/claim_worker.py`|组织独立进程、真实模型决策和上述协议操作|
|`demo/reliability_audit.py`|从公开证据重放策略、检测报告违反证据义务|
|`demo/import_reliability_decision.py`|外部模型结果与输入绑定；不修正模型决定|
|`demo/compare_reliability_policies.py`|同一提议的三策略消融|
|`demo/evaluate_reliability_comparison.py`|仅事后读取静态实验真值进行评分|
|`demo/reliability_preflight_bundle.py`|一条命令贯通离线交接流程|

以上路径均在`trust_network/`下。

完整业务流程可参考`run_invoice_claims.py`、`run_negotiation.py`、`run_claim_propagation.py`、`run_live_propagation.py`，**先读入口再执行**：一些旧脚本固定了输出位置、配置路径或历史实验来源，不要盲目运行覆盖旧记录。

已有数学模块`solve/attribution.py`、`settlement.py`、`global_dp.py`及相关game模块保留。它们不是直接从真实日志自动给出责任百分比的服务；不要为调用它们而编造概率、义务或损失归因参数。

## 4. 当前机制是什么，哪些还没完成

当前V20根据接收方可见的签名证据构造动作依赖，合并共享根证据查询，以接收方配置的受影响损失/查询成本排序。模型不能自行填一个低风险值绕过验证。受保护的账单批准必须检查必需证据；低风险转发可选择免除主动新鲜度查询。

每个权威答复绑定工作流、批次、根声明、相关提议和随机challenge。查到否认后冻结相关任务，无关任务继续。收到有效撤销证明后持久保存，后续批次不能忘记。派生声明还必须遵守可验证的转换规则。

`omission_weight`只是可选的 operational approximation，默认0，不等价于数学责任结算。签名只证明发布者和内容，不证明事实必然真实或发布者必然有过错。

**已知重要边界：**

- V20现成的三策略入口是固定提议下的“新鲜度选择消融”。`autonomous`仍保留结构与动作合约，**不能冒称完整Autonomous Agent基线**。
- 现成fixture是简化账单场景，正常/私有撤销条件用于接口实验；它不是完整多组织、多轮人工业务替代的最终demo。
- 完整模型多轮补证/恢复调度尚未接好，这是你可补的主要实验代码。
- 当前主要是本机独立目录/进程，部分历史模块支持HTTP；没有证明跨物理主机部署或OS级私有状态隔离。
- 查询与外部动作不原子，没有持久业务幂等保障。当前动作是模拟批准，不能称为真实付款或发货。
- 只验证明确的结构化声明，不验证任意自由文字。曾观察到模型结构化金额为分，解释却写成元。
- 最近已修复：已执行副作用后的ValueError必须记EFFECT_UNKNOWN；同批次已知撤销必须阻断低风险共享分支。不要回退这些修正。

## 5. 需要回答的研究问题

**Q1 程序介入：**面对真实LLM的批准提议，机制是否确实阻止了依据已失效事实的模拟执行？给出同一提议的对照轨迹。

**Q2 正常可用性：**正常任务会不会大量hold、升级或耗尽轮次？不能把这些当成安全完成。

**Q3 传播控制：**错误/失效声明到达了多少组织、被当作确认事实引用了几次、传播了几跳？区别模型看见文本与网络正式接受声明。

**Q4 局部恢复：**补证后能否恢复受影响任务，同时保持无关任务继续？是否确实重新生成并验证了派生声明，而非程序偷偷改值？

**Q5 合理追溯：**是否能区分原始权威声明错误、下游错误转换、撤销尚未送达、已收到否认仍继续使用？证据不足时是否明确不下结论？

**Q6 成本：**在上述安全和完成能力相近时，是否减少查询/补证/模型调用或token？没有减少也如实报告。

运行时门禁、签名、依赖图、撤销、两阶段提交本身不应作为我们的新发明。待验证的贡献是这些机制在跨组织协作中的联合调度、传播控制和恢复/追溯效果。不要未经文献核查宣称首创。

## 6. 执行顺序

### A. 离线接线检查：只做一次

```bash
.venv/bin/python -m trust_network.demo.reliability_preflight_bundle --out results/next_agent_preflight
```

目录必须是新目录。检查manifest为completed、审计能重放、评分分母明确。它是脚本提议测试，不能当真实模型结果。

发现会影响主结论的漏洞可以修复，并记录旧行为、修正、相应局部测试。不要因低影响格式问题扩展成全面重构。旧实验数据一律保留。

### B. 固定真实提议pilot：先产生明确因果证据

建议首次规模：正常/私有撤销2条件，每条件3个真实模型决定，共6个原始决定；每个决定重放3策略，共18次本地策略执行。重复调用与格式重试另计。pilot不代表统计充分。

具体步骤：

1. 用`prepare_reliability_demo`建立fixture。仅将`model_input.json`作为任务上下文交给业务模型；固定相同system prompt，不能透露policy、条件标签、目录名、真值、私钥或其他组织私有状态。
2. 必要时新增一个小型真实调用适配器，复用provider。保存实际发送内容、原始返回、parsed decision、模型标识、采样参数和用量。必须记录真实重试，不能只保存理想的第一次请求。
3. 每个原始模型决定只产生一次，三策略使用完全相同决定、签名证据、密钥与初始状态副本。模型hold或失败也要进入记录，不重抽直到approve。
4. 将模型记录按下面格式交给导入器，再执行比较与评分。

```json
{
  "input_hash": "对model_input.json对象调用documents.digest所得摘要",
  "model": "实际模型与版本",
  "status": "success",
  "decision": {"action": "approve", "claims": ["模型实际引用的摘要"], "reason": "模型原始理由"},
  "usage": {"prompt_tokens": 0, "completion_tokens": 0, "total_tokens": 0, "attempts": 1}
}
```

上例usage的0只是格式示意，实际必须填真实值；未知为null。调用失败用`status=provider_error`，不得填0伪装零成本。hold不要求claims。原始请求/响应可放记录额外字段，不放Authorization密钥。输入摘要绑定模型任务对象，额外system prompt也要原样保存。

```bash
.venv/bin/python -m trust_network.demo.import_reliability_decision --model-input FIXTURE/model_input.json --record MODEL_RECORD.json --out IMPORT_DIR
.venv/bin/python -m trust_network.demo.compare_reliability_policies --fixture FIXTURE --proposals IMPORT_DIR/proposals.json --provenance IMPORT_DIR/provenance.json --out COMPARISON_DIR
.venv/bin/python -m trust_network.demo.evaluate_reliability_comparison --comparison COMPARISON_DIR --truth FIXTURE/evaluation_truth.json --out SCORES.json
```

每次用新目录。比较中的`new_model_calls=0`仅指重放不新增模型调用，不表示原始决定没有成本。固定提议重放是因果消融，不能写成18个独立LLM样本。

**特别注意成本偏差：**同优先级根按摘要排序，随机密钥会影响短路顺序。必须固定配对初始状态，报告每次查询顺序；不能重新生成fixture挑选省查询的版本。

pilot完成后给出报告。若还需扩大付费规模，先给用户具体规模、预计实际调用上限、停止规则和要解决的不确定性，让用户决定。不要只问笼统的“是否继续”。

### C. 接入真实多组织、多轮流程：最终研究证据不可省略

B阶段是必要但不充分的接线/因果实验。不能以B成功就声称总目标完成。

复用真实账单或采购流程，至少包括来源组织、派生/协调组织、审批/执行组织，业务Agent产生实际API决策。权威内部审批或费率/批次状态不向其他组织直接公开。

需要一个模型无关的有界调度器，三个组获得相同任务、工具、公开证据和恢复轮数：

- **完整Autonomous**：模型自行选择查证/补证/继续/hold，拥有与其他组相同的查证工具；清楚定义其共同基础业务检查。
- **Verify-All**：对每项相关依赖执行验证，合理共享本轮重复证据，不人为重复查询以抬高成本。
- **我们的策略**：调用当前程序规划、局部冻结及恢复接口。必须在实际消息转发/执行前介入。

需要改接口时只补上述调度。不要给本组额外好prompt、更高轮数、基线看不到的公开信息或独有修复工具。实际跨组提议可不同，这是完整工作流对照；与B的固定提议结果分表。

恢复顺序：权威按其私有业务状态签发新证据→签署新旧证据关联offer→接收方prepare_recovery→原签发者重建受影响派生声明→模型重新决定→门禁再检查→记录模拟动作结果。程序不能替模型approve，不能清除旧revoked，EFFECT_UNKNOWN不能自动重试。

如需人工补证，实验控制器可注入预先定义的业务事件，必须明确标为受控事件。不要把合成故障称为模型自然错误。

多轮评估需要按事件序列建立独立真值；当前静态评分器不能直接用于状态变化中的整个工作流。评估器可以读真值，运行时与业务模型不可以。

## 7. 场景与信息边界自检

提交实验结果前逐条回答：

1. 公开字段是否已经直接显示错误？若是，只能作为结构错误条件，不能声称测试了私有事实查证。
2. 正式批准等关键事实是否确实由权威组织回答，而非LLM或全局控制器替其编造？
3. 模型原本打算继续时，程序是否能实际阻断转发/动作？只是追加责任文字不算。
4. 无关任务能否继续，正常条件能否完成？全部拒绝不算成功方案。
5. 真实模型是否获得了公平查证机会？不能通过隐藏本应公开的parents/rule让基线吃亏。
6. 运行时是否读过evaluation_truth或其他组织私有文件？组织服务自身读自己的状态正常；接收方直接读另一组织私有状态不正常。
7. 是否混淆本地进程、跨HTTP服务、跨物理设备和强隔离？按实际部署层级报告。

## 8. 指标与记录规范

主表首先给出计数及分母，再给比率：

- unsafe_completion_rate：最终不安全完成的**工作流**数/总工作流数；B阶段另标per_submitted_proposal，不能混用。
- 正常任务安全完成率、未完成/模型hold/超轮数/提供商失败/结果未知。
- 错误阻断数，区分模型自行阻断与程序覆盖模型放行。
- 错误声明触达组织数、正式接受/引用次数、传播跳数；失败调用可能已接收输入，不得计作零暴露。
- 补证后恢复率、无关任务受阻率。
- 查询数、补证数、升级率、模型实际API请求数与token；失败未知成本单列。
- 可审计事件数、不能判定的责任情况、报告与公开证据不一致的实例。

若数据规模不足，明确小样本，不要报精确的一般化可靠性结论。同一提议的多次重放不是独立样本。不能删除失败行，不能用零分母0替代null。

每个run保留：源码摘要、配置、条件/重复编号/策略、完整冻结请求与响应、提供商元数据、签名声明/答复/事件、最终模拟动作、独立评分和异常阶段。对日志对象做快照，避免后续追加列表改变过去请求，仓库历史上已发生过这类错误。

## 9. 已有证据，不要重复发明历史

以以下实际文件为准，历史结论不是当前源码已验证证明：

- `results/repair_v10_unified/report.md`：18条真实对照，自主不安全4/6，完整验证0/6，依赖阶段0/6；安全完成分别2/6、2/6、4/6。条件为受控业务变更，每条件样本小；组间模型提议不同，不能全归因于机制。
- `results/paired_repair_v12/report.md`：反提案候选没有带来稳定增益，保留负结果。
- `results/propagation_v19_visible_dependencies/report.md`：正常两跳可通过；金额错误被模型自行挡住，遗漏依赖分支调用失败，没有新增错误再传播证据，不能声称程序增加了最终安全性。
- V20本地preflight展示程序阻断私有撤销与正常分支继续，但使用脚本提议，不是新真实模型证据。旧manifest可能早于最近修正，不覆盖当前版本。

若这些文件在你收到的仓库缺失，就标记缺失，不把本文数字当成重新验证过的结果。

## 10. 最终交付与停止条件

交付到新目录，例如`results/next_agent_experiments/`，至少包含：

1. `experiment_manifest.json`：研究问题、组定义、条件、计划样本量、停止规则、代码摘要、模型与参数。
2. 原始逐run数据及审计/评分输出，目录不能含API密钥。
3. `metrics.json`和`report.md`：分母明确的主表、正负结果、错误案例、模型/系统失败及成本。
4. `mechanism_cases.md`：至少一条实际可追踪的拦截/恢复轨迹；若没有出现，明确写没有，不拼造案例。
5. 如补了调度器，提供单一运行命令及必要局部测试；不要只留下不可重放的临时脚本。
6. 明确回答Q1—Q6，指出已证明、仅初步观察、尚未验证三类结论，并给出下一步最值得做的一项实验。

不要因为有正结果就继续调参强化故事，也不要因为负结果就偷偷换场景。遇到核心问题时分析它属于：模型已经自己解决、机制未进入关键路径、场景缺少决策空间、保守阻断损害完成率、恢复失败、评估偏差或提供商故障。保留失败证据，再提出明确修改。

**完成标准不是“测试通过”，而是得到公平、可复现、能解释机制是否有增量价值的真实实验结论。负结论也可以完成一轮实验；不能因此宣称总体研究目标已经实现。**
