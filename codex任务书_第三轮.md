# 给编码Agent的第三轮任务书

## 本轮要回答的问题,以及为什么

这个项目最终要证明的是两层东西:第一层(主目标)是"某种协议/机制设计能不能实质性地增加跨组织agent网络的正确性、减少错误信息和错误行为被放行的概率";第二层(支撑目标)是"错误可不可以被检测、可不可以被追责"。第二轮已经在**图上的数值模型**里证明了第一层——遗漏责任规则(`ProportionalWithOmission`)让信用证场景的PoA从2.638降到1.006,检查比从0恢复到社会最优水平。但这个结果目前只存在于抽象的数值仿真里,还没有在真实的LLM agent身上验证过:现有的MiniMax demo虽然跑了30个run,但用的仍是第一轮的旧`resp`提示词,压根没有把新设计的遗漏责任规则告诉真实agent。

**本轮的核心任务只有一件事**:把遗漏责任机制真正接入MiniMax demo,做一次小规模、对照干净的实验,直接测量——在有遗漏责任激励和没有这个激励两种情况下,真实的银行审单agent,有没有更大概率真的把那个真实存在的MX-40/MX-40B发票版本冲突揪出来。这一步做完,项目才算真正把"数学上有效"接成了"真实agent上有效"这条完整的证据链。

本轮不追求大样本、不追求统计显著性、不铺开扫描——按"验证只做必要程度"执行。

---

## 优先级1(本轮主线):遗漏责任机制接入真实demo + 真实检出率对照

### 需要新增的东西:resp-omission任务框架

现有demo里`selfish`/`resp`两种目标是通过**任务目标描述文本**体现给LLM的,不是把数学公式喂给它。本轮需要在此基础上,新增第三种框架`resp-omission`,专门给**有审单能力的组织**用(即信用证场景里的`export_bank_review`和`issuing_review`,这是唯一真正具备"可以选择要不要深入检查"这个能力的两个组织,遗漏责任规则本质上只对它们有意义)。

`resp-omission`相对现有`resp`,只增加一层框架性的说明:**如果本组织在自己的审查范围内、本有能力核实某项内容却选择不深入核实,而这项内容后来被证实存在问题,本组织需要为此承担相应比例的下游责任,即使问题不是本组织直接造成的**。

严格约束:

- 这段新增框架**只能传达责任结构本身**,不能出现任何暗示具体检查什么、暗示型号、版本号、发票细节的措辞——不能变相把答案泄露给agent,否则测出来的"检出率提高"只是我们自己在提示词里透题,不是机制真的在起作用。
- 除了这两个审单组织的任务框架被替换成`resp-omission`,**其余所有内容(其他四个组织的任务描述、组织档案、公开单据、事故触发条件、protocol可见性设置)必须与第一轮完全一致**,不能顺带改动别的变量。
- 两个对照条件之间**只能有这一处framing差异**:
  - **条件A(baseline)**:两个审单组织沿用第一轮的`resp`框架
  - **条件B(omission)**:两个审单组织换成`resp-omission`框架
  - 其余组织在两个条件下都保持`resp`(选用第一轮已跑过的、信息最完整的`protocol2`可见性设置作为固定背景,不在本轮引入协议变量,避免和framing变量混在一起解释不清)

**在真正开始跑批次之前**,把条件A和条件B里这两个组织的任务描述原文完整地写进`MODEL_DECISIONS.md`,并列出逐句diff——这一步是给我们自己审查的,确认新增的部分没有夹带任何内容线索,再往下跑。

### 核心指标:真实检出率,不是完成率

现有的完成/升级/拒绝这一套指标不够——一次流程"完成"了,不代表那个真实存在的MX-40/MX-40B冲突真的被发现过,也可能是没人查出来、单据糊里糊涂放行了。本轮需要新增一个更贴近研究问题本身的指标:

```
flagged_invoice_discrepancy(run) ∈ {True, False}
```

判定这个值,优先复用现有审计日志里已经结构化记录的"检查范围/业务决策"字段(第一轮/第二轮的输出里都提到过这类结构化字段),而不是对自由文本做关键词匹配——如果结构化字段已经能支撑这个判定,直接用;如果现有字段不够精确,需要新增一个专门的、有明确判定规则的抽取步骤,并且这个规则写进`MODEL_DECISIONS.md`,保证条件A和条件B用的是同一套判定标准(不能针对某一组临时放宽或收紧)。

这个指标是本轮**唯一的主结论指标**。完成率、升级率、重提交次数、检查范围频率仍然要报告,但作为背景信息,不能拿来替代或者稀释检出率这个核心问题。

### 规模与执行前置检查

- 两个条件各**N=6次**,温度取0.2/0.5/0.8各2次(比第二轮的15次规模明显收紧,符合"验证只做必要程度")
- 执行前先输出预估调用量和token量(参照第二轮实际数据换算),等待确认后再跑,不要默认一次性执行完
- 沿用现有规则:网络失败/无效响应不伪造完成,失败run单独记录,不并入检出率统计的分母
- 不向任何agent上下文注入ground truth,评估器仍然只在运行结束后读取真值

### 验收标准

- [ ] `resp-omission`框架仅应用于`export_bank_review`、`issuing_review`两个组织,其余一切保持第一轮配置不变
- [ ] 条件A/B的完整任务描述原文和逐句diff,在跑批次**之前**已写入`MODEL_DECISIONS.md`,供审查确认无内容泄露
- [ ] `flagged_invoice_discrepancy`判定规则明确、可复现,两个条件使用同一套标准,判定依据写明是复用已有结构化字段还是新增规则
- [ ] 执行前输出预估调用/token量,等待确认后再执行完整批次
- [ ] 两个条件各6次、共12次run执行完毕,失败run单独记录不并入统计
- [ ] 报告以检出率为主结论(条件A vs 条件B各自的检出次数/6,附小样本区间,和第二轮同样的描述性区间风格),完成/升级/拒绝/重提交/检查范围频率作为背景信息一并列出
- [ ] 明确写清楚样本量限制:N=6/组不足以下因果或统计显著性结论,只能报告方向和幅度,不能措辞上暗示"证明有效"
- [ ] 明确写一句边界声明:这是遗漏责任机制第一次在真实agent上做的小规模检验,不是决定性证据,更大样本或更多场景变体是后续工作

---

## 优先级2(次要,可选,不需要新实验,直接用现有数据分析)

背景:第二轮发现遗漏责任规则在信用证场景(PoA 2.638→1.006)上效果显著,在随机图场景(尤其种子11)上几乎没反应。这一项只是想知道能不能不靠"多跑几组sweep撞运气",而是直接从图结构本身判断这套机制会不会奏效。

任务:对现有的4个场景(3个随机种子图 + 信用证图,全部复用已有配置,不新增图),给每个场景里的verify类型节点算一个"下游价值集中度"指标——即该节点所在路径下游承担的期望损失,占全图期望损失总量的比例。然后把这个集中度和第二轮已经算出的"PoA改善幅度(w=0到w=1)"放在一起看有没有对应关系。

这一步全部基于已有结果,不需要新跑sweep,不需要新的图。

### 验收标准

- [ ] 明确写出集中度指标的定义
- [ ] 给出4个场景各自的集中度数值和对应的PoA改善幅度,做一次对比(n=4,明确说明这只是探索性观察,不是统计检验)
- [ ] 如实报告:这个对比是否支持"瓶颈节点假说";如果n=4看不出明显规律,说清楚需要什么样的场景(不需要在本轮生成)才能进一步检验,留作后续问题,不在本轮强行下结论

---

## 整体要求

- 两个优先级都要在`README.md`里补一节说明本轮新增内容和复现方式
- 现有测试(第一轮24项+第二轮新增)必须继续全部通过
- 不覆盖`results/`下已有的任何第一、二轮文件
- 结论无论正面反面都要如实写进`DELIVERY_v3.md`
