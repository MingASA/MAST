# 第三轮任务书补丁：暴露受控变体

## 触发原因

执行前历史回放发现：第二轮30个run、44次银行决策中，暴露于未修正冲突的次数为0（卖方已在银行前修改发票，或流程提前终止）。严格沿用原流程执行A/B批次，大概率产生双组零检出、零暴露，无法回答"银行框架变化是否改变其检查行为"这个问题。**因此暂停原方案，不要执行原12-run批次。**

## 修改内容

只改一处：银行组织（`export_bank_review`、`issuing_review`）收到的起始单据输入，从"依赖卖方agent自身决策链路生成"改为**固定写死为已确认包含MX-40/MX-40B冲突的版本**，跳过卖方是否自我修正这一不确定环节。除此之外：

- 不改动卖方（或其他任何组织）自身的任务描述、行为逻辑、角色顺序
- 银行组织的下游流程（是否补证、是否升级、是否重提交）保持原有真实决策，不做任何预设或引导
- `resp` / `resp-omission` 两种框架的内容和适用范围维持第三轮任务书原定义不变
- A、B两组必须使用完全相同的这个固定受控输入，唯一变量仍是银行组织拿到的是`resp`还是`resp-omission`框架

## 报告要求（新增）

- 明确将这批结果标注为**"暴露受控"实验**，只回答"银行面对该冲突时，框架是否改变其检查/查证行为"，不代表、也不能被引用为"银行在自然流程中遇到此类冲突的概率"
- 报告中需并列写出第二轮历史回放的0/44这个背景数据，说明为什么本轮改成暴露受控设计
- 其余验收标准（N=6/组、温度分层、执行前预估调用与token量并等待确认、检出率为主指标、失败run单列不入分母、样本量限制声明）与第三轮任务书原文完全一致，不再重复

## 执行前确认

请先给出：这个固定受控输入包的具体内容摘要（确认与`ground_truth.json`里定义的冲突一致）、修改后的预估调用/token量（预计与原估算量级相近），我确认后再执行。
