# 给编码Agent的第二轮任务书

本文件是第一轮交付（见仓库 README.md / DELIVERY.md / MODEL_DECISIONS.md）的延续，不是推倒重来。请先阅读现有 README、DELIVERY、MODEL_DECISIONS 三个文件，理解当前代码结构和已经做出的修正，再开始本轮工作。本轮所有改动必须是**增量式**的：不改动、不覆盖已通过的24项测试，新结果写入新的版本化输出目录，不覆盖 `results/` 下已有文件。若某项要求与现有代码结构冲突，请像上一轮一样：做出合理判断，把偏离和理由记入 `MODEL_DECISIONS.md`，而不是为了凑合而扭曲实现。

本轮包含三个优先级，彼此独立，可以分别验收，但优先级1和2共享同一批实验基础设施，建议先做完1再做2。

---

## 优先级1：机会性责任结算规则 + 过度验证监测

### 背景（为什么要做这个）

上一轮的核心发现：默认结算规则（`Proportional`）只对"起源节点"和"检查了但漏检"（F集合）分配责任，"本有机会检查但没检查"这件事本身不产生任何额外责任风险，因此责任机制没有改变任何组织"要不要验证"的边际收益，六组组合下PoA均未改善。本优先级的目标是：设计一个把"未检查"本身也纳入责任分配的规则变体，重新做实验，**如实报告**它是否真的能压低PoA、以及会不会带来新的副作用（过度验证）。

### 形式化规格

新增一个可插拔的结算规则 `ProportionalWithOmission`（与现有 `Proportional`、`BearerOnly` 并列，不替换它们）。

先定义"机会性未检查"这个概念。对路径上任意一个决策点 v（即：该组织的策略在此处本可以选择verify，但选择了不verify），定义：

```
available(v)  = 在该状态下verify在结构上可达且预算允许（即 Q_verify(v,b,r) 在当时的(b,r)下是可行选项，不是被预算或路由排除）
omitted(v)    = available(v) 为真，且实际策略在此处选择了pass而非verify
```

给"机会性未检查"分配的责任权重，用**后验校正过的**"如果查了、本来有多大概率能拦住"来标定：

```
omission_weight(v) = P(X_v = 1 | 观测)  *  delta_v  *  1[omitted(v)]
```

其中 `P(X_v=1 | 观测)` 直接复用 `solve/attribution.py` 里已经实现的后验推断（不要另起一套），`delta_v` 是该verify类型本应具备的检出率（即使没有被调用，其参数在环境定义里仍然已知，这是defender侧参数，不是"这个节点从未存在"）。

`share_i(v_loss)` 的计算方式扩展为三项加权：

```
raw_score_i = w_origin * P(K* 落在 i 拥有的节点)
            + w_miss   * P(i 拥有的节点落在 F 集合)
            + w_omit   * sum_{i拥有的节点 v} omission_weight(v)

share_i(v_loss) = raw_score_i / sum_j raw_score_j
```

`(w_origin, w_miss, w_omit)` 是暴露出来的超参数，不要写死。要求至少跑以下几组：`w_omit ∈ {0, 0.5, 1.0, 2.0}`（`w_origin, w_miss` 固定为当前默认值不变），其中 `w_omit=0` 必须**精确退化为现有 `Proportional` 规则的数值结果**——这是最重要的回归检验，用来确认新实现没有在 `w_omit=0` 时意外改变原有行为。

### 新增指标：过度验证比

```
over_check_ratio = E[均衡下实际执行的verify次数] / E[社会最优解下的verify次数]
```

需要在现有 `experiments/run_sweep.py` 的输出表里，给每一行（场景×协议×目标×起点）都加上这一列，不是只在新规则下才报告——旧规则（`Proportional`、`BearerOnly`）的历史结果也应该补算这一列，方便横向对比。

### 验收标准

- [ ] `ProportionalWithOmission` 通过现有的 `Σ_i share_i(v)=1` 单测和"固定策略下更换结算规则、社会成本不变"单测（把这两个测试参数化，覆盖新规则，不要只测旧规则）
- [ ] `w_omit=0` 时，PoA 和 over_check_ratio 与现有 `Proportional` 规则的历史数值完全一致（给出具体对比数字，不是"应该一致"这种断言）
- [ ] 重新跑 sweep：现有3个随机种子场景 + 信用证场景 × 协议(0/1/2) × 目标(selfish, resp-Proportional, resp-Omission@四个w_omit取值) × 现有的2个起点，全部输出到新的版本化目录（如 `results/sweep_v2_omission/`），不覆盖 `results/`
- [ ] 汇总一张表：每一行是 (场景, 协议, 目标/w_omit) 组合，列包含 PoA 和 over_check_ratio
- [ ] 如实报告：PoA 是否随 w_omit 增大而单调下降？over_check_ratio 是否随之上升？如果存在这个此消彼长的权衡，明确写出来；如果某个w_omit同时降低PoA又没有明显推高over_check_ratio，也如实写出来——不要预设"责任机制应该有效"然后挑数字，两种结果都是合法产出
- [ ] `MODEL_DECISIONS.md` 记录：`available(v)` 在实际代码里具体是怎么判定的（尤其是verify作为节点类型还是节点上的二元动作，这两种实现下"机会"的定义可能需要不同的落地方式，如实说明你采用的映射）

---

## 优先级2：信用证场景高PoA的结构消融

### 背景

信用证场景observed PoA≈2.64，明显高于随机图场景的1.09–1.16。信用证场景专门插入了 `decision_maker(v) != bearer(v)` 的配置（出口方银行决策、卖方付费、买方承担默认损失）用来检验"决策权与责任分离处系统性验证不足"这一预测。目前README明确说"不能据这一配置预先断言必定验证不足"——本优先级就是去补上这个尚未做的因果检验。

### 具体任务

在 `scenarios/letter_of_credit.py` 里，先明确列出当前所有 `decision_maker(v) != bearer(v)` 的节点（可能不止一处，请逐一列出，不要假设只有一处）。针对每一处不一致，构造消融梯度，其余参数（拓扑、α/p/c/L、δ/ε、预算）**完全不变**，只改角色映射：

- **(a) baseline**：现状，保留
- **(b) decision-payer分离，decision-bearer对齐**：把该节点的 `bearer` 改成等于 `decision_maker`（`payer` 保持不变，仍可以和decision_maker不同，用来单独检验"决策-承担"这一条链的影响，排除"决策-付费"分离的干扰）
- **(c) 完全对齐**：`decision_maker = payer = bearer`，作为"这条链上没有任何角色分离"的对照上限

如果存在多处不一致的节点，(b)/(c) 应该分别对每一处单独消融一次（保持其他不一致点不变），而不是一次性把所有点都改掉——否则无法判断到底是哪一处分离在起作用。

### 验收标准

- [ ] 明确列出信用证场景当前所有 decision_maker≠bearer 的节点清单（写入输出报告，不只是代码里隐含）
- [ ] 每个不一致节点分别生成 (a)/(b)/(c) 三个场景变体，(b)/(c) 场景配置存为独立的JSON配置文件，不覆盖原有的信用证场景配置
- [ ] 对每个变体重新跑完整sweep（协议0/1/2 × selfish/resp × 两个起点），复用优先级1里加了over_check_ratio列的sweep流程
- [ ] 汇总对比表：每个消融节点的 (a)/(b)/(c) 三行 PoA 和 over_check_ratio，横向对比是否随着角色对齐程度提高、PoA是否向随机图的1.09–1.16区间靠拢
- [ ] 如实报告结论：如果PoA确实随对齐程度下降，这是对我们最早场景设计预测的一次数值验证，明确写出降幅；如果PoA没有明显变化，同样如实写出来，并可以推测（不需要证明）PoA的主要来源可能是别的结构因素（如链条深度、单一关键路径），留作后续问题，不需要在本轮解决

---

## 优先级3：MiniMax demo 规模化到有统计意义的重复次数

### 背景

现有MiniMax demo每个组合只跑了一次，只验证了端到端可运行性，不能用来估计事故率、升级率或校准数学参数。本优先级把同一个事故链场景（10套工业泵、MX-40/MX-40B发票版本冲突）在**保持场景和ground truth不变**的前提下重复运行多次，唯一变化来源是LLM本身的随机性（不同random seed/temperature），用来得到一个真实的结果分布，为以后校准数学参数打基础——**本轮不做参数拟合**，只产出分布本身。

### 重要：先估算成本，再执行

单次六组合demo已知消耗约39次调用、71645 token。重复N次 × 完整6组合，调用量会线性放大。**在真正发起完整批量调用之前，先只根据已知的单次调用量做一个预估（预估N×组合数×调用次数、预估token总量），把这个预估数字打印或写入报告，明确等待确认后再执行完整批次**，不要默认一次性跑满。

默认建议范围（仅为默认建议，若预估成本过高可以主动提出压缩方案，不要不声明就擅自缩小范围）：

- N = 15次重复
- 先只跑对比反差最大的2-3个组合（建议：`protocol0 × selfish` 与 `protocol2 × resp`，一个信息最少+无责任，一个信息最全+有责任，最具对比性），而不是全部6组合都乘以N
- 每次重复只改变随机种子/temperature，场景输入（组织档案、公开单据、事故链触发条件）保持完全一致

### 验收标准

- [ ] 复用现有 `demo/run.py` 及其现有的API错误处理规则（网络失败/无效响应不伪造完成，`.jsonl.interrupted` 保留中断的部分调用），扩展出一个批量运行脚本（如 `demo/run_batch.py`），支持指定N次重复和组合子集
- [ ] 执行前输出预估调用总量和预估token总量，附上"基于单次运行39次调用/71645 token"这个已知基准的换算依据
- [ ] 完整批次运行后，产出聚合报告（CSV/JSON），至少包含：完成/升级/拒绝结果的计数分布、重提交次数分布、检查范围选择的频率分布、每次运行的API调用次数分布、私有标记泄露命中次数（跨全部完成的run汇总，不是只报告有没有命中一次）
- [ ] 未完成/报错的run必须单独记录，不能并入"完成"统计口径，保持与现有规则一致
- [ ] 报告中明确写一句边界声明：这批结果仍然只是行为分布的经验观测，不是数学模型参数的拟合值，参数校准是后续工作

---

## 整体交付要求

- 三个优先级的新增代码、新增测试、新增场景配置文件都要有清晰命名（避免与第一轮产出混淆），并在 `README.md` 追加一节说明本轮新增了什么、如何复现
- 现有24项测试必须全部继续通过
- 三个优先级的结论无论是正面还是负面，都要在 `DELIVERY.md`（或新增一份 `DELIVERY_v2.md`）里如实写出来，不要因为某个方向没有得到预期效果就淡化或省略
