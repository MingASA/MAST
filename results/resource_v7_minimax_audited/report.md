# 修复后的真实 HTTP 资源流程

本次3次 MiniMax 调用，2521 token，8次 HTTP 请求，最终 completed。买方真实生成运费2100的方案；事后读取供应、运输及买方私有状态，硬约束检查均无违反。运行时协调方没有读取这些私有状态。

独立 audit_resource_run 重建模型输入摘要、买方 propose、批准范围及有效期、两方准备票据、签名提交决定和两方执行确认，通过。结果见 independent_audit.json；私有状态事后评估见 evaluation.json。这是本轮首个通过完整上述检查的真实HTTP资源执行，不替换或修饰此前失败记录。

本轮三个真实尝试合计9次模型调用、7590 token：首试2541 token因接线错误中断，第二次2528 token执行成功但输入日志审计失败，本次2521 token执行及上述审计通过。不能只统计最终成功重跑成本。

边界：单个真实样本、无对照、独立本机进程。签名链依赖预置信任的公钥和诚实协调服务时钟；不证明恶意组织如实报告库存或实际物理发货。资源服务临时目录生命周期不提供生产级持久恢复。此结果支持实现已接通，不支持统计可靠性或成本优越性。
